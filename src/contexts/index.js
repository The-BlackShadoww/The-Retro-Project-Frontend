import { createContext } from "react";

export const SearchContext = createContext(null);
export const BagContext = createContext(null);
export const MemberProfileContext = createContext(null);
export const AllAccountContext = createContext(null);
export const CurrentAccountContext = createContext(null);
export const AllProducts = createContext(null);
export const AuthToken = createContext();
export const MemberId = createContext();
export const LocationContext = createContext();
